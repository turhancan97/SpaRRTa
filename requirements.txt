# Core dependencies for the SpaRRTa spatial-relation probing benchmark.
# Install PyTorch separately following https://pytorch.org for your CUDA version, e.g.:
#   conda install pytorch=2.2.1 torchvision=0.17.1 pytorch-cuda=12.1 -c pytorch -c nvidia

torch>=2.0
torchvision>=0.15

# Config system
hydra-core>=1.3
omegaconf>=2.3

# Backbones / model loading
timm>=1.0.0
transformers>=4.29
open_clip_torch>=2.23.0
einops>=0.7.0

# Data / numerics / metrics
numpy>=1.21
scikit-learn>=1.0
opencv-python>=4.5
pillow>=9.0

# Plotting & logging
matplotlib>=3.5
loguru>=0.6
tqdm>=4.50

# Weight downloads
gdown
wget

# Optional: experiment tracking (enable via wandb.use=True)
# wandb

# Optional: required only for the maskfeat_vitb16 backbone
#   pip install -U openmim && mim install mmcv mmcls "mmselfsup>=1.0.0rc0"
